import {render, screen, cleanup} from "@testing-library/react"
import Login from "../Login"
import renderer from 'react-test-renderer'
import "@testing-library/jest-dom/extend-expect"

afterEach(()=>{cleanup();})

test("Test1 for login",()=>{
    
})